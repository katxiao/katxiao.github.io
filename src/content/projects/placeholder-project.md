Write your project writeup here in Markdown. This file lives at `src/content/projects/your-slug.md`.

Describe the motivation, the build process, and what you learned. Use images and video in the data file (`src/data/projects.js`) — they'll appear above this text on the project page.

## Section headings

Use `##` headings to break the writeup into sections: motivation, how it works, materials, next steps, etc.

## Code snippets

Inline `code` and fenced code blocks work too:

```python
def hello():
    print("hello")
```
